# 2026-07-13 工作日志

## 自动化日报生成 v1
- **窗口**: 2026-07-12 (周日)
- **采集**: WebSearch 多关键词，9条新闻
  - 政策法规(1): 北京智驾保险
  - 标准动态(1): GB 47955-2026正式发布
  - 投融资动态(0): 暂无更新
  - 技术动态(2): 特斯拉FSD v14 Lite韩国更新、零跑城市NOA下沉至8万元级
  - 项目动态(3): 小鹏Robotaxi内测+兼任机器人负责人、特斯拉Cybercab员工试乘、京津冀产业链对接
  - 行业动态(2): L2搭载率70%、特斯拉产线改造为Optimus
- **脚本**: score_articles ✅ / gen_report ✅ (38KB) / import_news_to_db ✅ (新增9条)
- **公众号采集**: 跳过（playwright 未安装）
- **特点**: 周日窗口新闻较少，投融资类暂无更新

## 自动化日报生成 v2（公众号补全）
- 安装 playwright + Chromium 后补跑公众号采集
- 公众号新增 1 条 → 项目动态：自驾车早《美监管机构警告Robotaxi避让缺陷，文远车队破千辆》
- 重新评分 ✅ / 日报 v2 ✅ (10条, 40KB) / 入库新增 1 条（累计36条）

## Web 入口改版 — 统一报告入口
- 新增 `/api/reports` 端点：扫描 reports/ 目录，返回按类型分组的报告列表
- 新增 `/reports/<path>` 路由：提供报告 HTML 文件访问
- 重写前端为单页应用：
  - 顶部导航：日报 / 周报 / 月报 / 数据查询
  - 日报 tab：按月分组展示往期报告卡片，含版本/大小/最新标识
  - 周报 tab：展示已有周报
  - 月报 tab：提示即将上线
  - 数据查询 tab：保留原有统计/图表/筛选/文章浏览功能
- 设计风格：深色汽车科技风，金色强调色，圆角卡片布局

## 弹窗跨 iframe 通信 + 链接新窗口打开
- 周报弹窗从 iframe 内部移到父页面，通过 `postMessage` 跨 iframe 通信
- 父页面 `index.html` 新增全局弹窗 `#articleModalOverlay`
- 弹窗打开时不再滚动父页面到顶部，保持用户阅读位置
- 日报标题链接 `target="_blank"` 新窗口打开原文

## 日报/周报新增概览板块
- 日报模板 `daily_template.html` + 脚本 `gen_report.py`：stats bar 和 main 之间新增"今日概览"
- 周报脚本 `generate_weekly.py`：hero 之后新增"本周概览"
- 格式：卡片网格布局，每类有标题+bullet列表，每条截断至55字
- 已重新生成日报 v6 和周报 W28/W29

## 概览文本入库体系
- `articles` 表新增 `overview` 列
- 新建 `scripts/generate_overview.py`：从标题中提炼简短概览（含冒号标题取"主体：内容"格式，其余截断至30字）
- `gen_report.py` 改从 `overview` 字段读取（不再截断标题）
- `generate_weekly.py` 改从数据库 `overview` 字段读取
- `import_news_to_db.py` 新增 `overview` 字段写入
- 已补全历史 36 条数据的概览文本
- **每日流程新增步骤**：`merge_public_accounts.py` → **`generate_overview.py`** → `gen_report.py`

## gen_report.py 改为从数据库读取
- 去掉了 `news_index.json` 依赖，直接连 SQLite 按 `window` 字段查询文章
- `build_overview` 移除（日报已去掉概览）
- `load_data()` 替换为 `load_from_db(window_date)`，按窗口日期查询
- 命令接口不变：`gen_report.py <日期> <版本号> [窗口日期]`

## 日报/周报统一 Warm Editorial 设计
- 重写 `templates/daily_template.html`：暖白底色 #f8f6f2、琥珀色 #c27803 强调、DM Serif Display 标题
- 重写 `gen_report.py`：匹配新模板 CSS 类名（article-card/overview-cat-title/stat-chip），去掉 stats-bar 改为 stat-summary 标签
- 重写 `generate_weekly.py` 的 `render()`：CSS 和 HTML 结构与日报完全一致
- 统一结构：Hero（badge+title+date+统计标签）→ 概览（扁平列表）→ 分类新闻（统一卡片样式）→ Footer
- 日期显示修复：每个日报正确显示对应日期
